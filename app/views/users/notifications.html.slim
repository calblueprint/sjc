= render "partials/header"

.page-bar
  .container
    .page-bar-title Notifications
    .page-bar-left
      button.button Mark as read

.container
  = react_component "NotificationsList", {  \
      userId: current_user.id               \
    }